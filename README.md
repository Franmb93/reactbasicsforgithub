# Covering React Basics

This project was created to cover the React basics and use it as a 'tutorial for React'

# To install it
Use `npm install` on the terminal

# Run it

Just use `npm start` on the terminal.